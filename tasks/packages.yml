- name: Install htop
  dnf: 
  name: "{{ item }}"
    # TODO - split out packages into 'functional units' (ex: setup desktop,
    # cpp development, video, gaming, etc.)
  loop:
    - htop
    - tmux
    - ffmpeg-free
    - vim
    - nodejs
    - keepassxc
    - cronie
    - cronie-anacron
    - virt-manager

